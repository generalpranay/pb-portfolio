{
  "name": "pb-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"client,server\" -c \"cyan,yellow\" \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server"
  },
  "dependencies": {
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
